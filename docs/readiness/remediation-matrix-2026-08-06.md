# Quantivis Readiness Remediation Matrix

Baseline date: 2026-08-06

Baseline commit: `0880f4281a08bc42ce841c95d1468f7875e3f2c4`

Target branch: `main`

Source audit: public live readiness audit dated 2026-08-06

Status vocabulary: `Verified in production`, `Verified locally`, `Code-complete, deployment unverified`, `Blocked by external configuration`, `Not ready`.
`Code-complete, deployment unverified` never means deployed, pilot-ready, or GA-ready.

## Baseline release gates

| Gate | Baseline evidence | Status |
|---|---|---|
| Production build | `npm run build` passes; chunk-size warnings remain | Verified locally |
| Unit/integration suite | 832/832 passing as of this commit (was 8 failures at baseline `0880f428`; fixed by subsequent commits) | Verified locally |
| Lint | 732 errors and 52 warnings at baseline | Not ready |
| Dependency audit | 8 vulnerabilities: 4 high, 3 moderate, 1 low at baseline | Not ready |
| Security configuration | `npm run verify:security-config` passes at baseline, but did not inspect stale Cloudflare scripts for `unsafe-eval` | Not ready |
| Production smoke | Audit observed broken free analysis and unavailable telemetry | Not ready |

## Critical and High findings

| Finding | Severity | Root cause | Affected files/services | Planned fix | Required tests | Production validation | External dependency | Status |
|---|---|---|---|---|---|---|---|---|
| Public demo-data analysis fails and UI can remain misleadingly busy | Critical | Single unbounded client request; no cancellation/idempotency; upstream 402 translated poorly; dependency configuration/credit state not surfaced safely | `src/pages/FreeAnalysis.tsx`, `supabase/functions/strategy-session`, Lovable AI Gateway | Bounded input; client/server timeouts; cancellation; stable request/idempotency IDs; bounded retries; rate limit; structured failure codes/logs; always recover UI | Unit tests for success/retry/failure; source contract test; browser smoke | Run demo data repeatedly; verify stream completion, cancellation, retry, error recovery, request ID and latency logs | Valid `LOVABLE_API_KEY`, provider capacity/billing, edge deployment | Code-complete, deployment unverified |
| Runtime stack is advertised beyond deployed maturity | Critical | Deterministic libraries and tests exist, but several runtime components have no deployed consumer | `src/lib/trust-center.ts`, runtime libraries, product/docs pages | Either wire a complete tenant-scoped runtime path or mark each component Beta/Demo/Roadmap/Not available everywhere | Runtime contract, authz, queue, persistence, recovery and E2E tests | Prove data-to-decision-to-outcome in deployed environment | Runtime infrastructure, production credentials, load environment | Not ready |
| Public status has no useful telemetry/scheduler evidence | Critical | Production function/RPC deployment or secrets/migrations unavailable; health-check source also contained invalid duplicated control flow | `supabase/functions/health-check`, `public-system-status`, `cron_run_log`, `/system-status` | Repair function; expose sanitized dependency and scheduler evidence; keep unknown when absent; add SLO checks | Function contract and status-derivation tests | Probe liveness/readiness; verify job execution IDs, last success/failure, duration and next run | Edge deployment, scheduler execution, monitoring/alert destinations | Code-complete, deployment unverified |
| Version `0.0.0` and missing build/deploy provenance | Critical | No build-time injection or release baseline | `package.json`, `vite.config.ts`, Enterprise Trust UI, CI/CD | Semantic prerelease version; immutable commit/timestamp; host deployment ID when supplied; migration version; no false deployment claim | Trust Center and release-provenance tests; build | Confirm live page matches deployed commit and hosting deployment ID | Hosting platform must supply deployment identifier | Code-complete, deployment unverified |
| Tenant isolation/RBAC proof absent | Critical | Protected routes exist, but evidence requires multiple real test identities and direct database assertions | RLS migrations, sensitive RPCs/functions, `tests/tenant-isolation` | Run two-org/multi-role negative tests; add missing endpoint/database checks; publish safe summary | Owner/admin/executive/analyst/viewer horizontal and vertical denial tests | Execute against isolated staging/production-like database | Test credentials and approved environment | Blocked by external configuration |
| Trust metrics show retention, data quality and drift at zero/unset | High | Root-caused 2026-08-06: no `cron.schedule()` entry invoked `supabase/functions/compute-trust-metrics`, so `trust_metrics_snapshots` stayed empty and the live Trust Center showed "No trust snapshot yet" / zero values. Follow-up audit found the function also contained favorable defaults: missing RLS/audit/explainability/intervention/retention/connector/DQ/drift evidence could become `100%`, zero, or a threshold-passing procurement status instead of unknown | `supabase/functions/compute-trust-metrics/index.ts`, `supabase/migrations/20260806083000_schedule_compute_trust_metrics.sql` | Added the daily authenticated pg_cron schedule and changed the metric computation to fail closed: query failures and absent evidence remain `null`/unknown, readiness labels render "unknown", and nullable evidence maps to `missing` instead of passing | `src/test/compute-trust-metrics-scheduled.test.ts`, `src/test/trust-center-connector-health-fake-100.test.ts`; build/full suite to rerun after this tranche | Confirm the migration is applied to the live database, the Edge Function is deployed, and a real `trust_metrics_snapshots` row appears after the next scheduled run (05:00 UTC) with provenance that matches production tables | Migration/function deployment requires working Supabase deploy credentials — same deployment-pipeline gap noted in "Deployment evidence and external blockers" below (`SUPABASE_ACCESS_TOKEN`/`SUPABASE_PROJECT_REF`) | Code-complete, deployment unverified |
| Evidence pack lacks real PDF/signing verification | High | Current implementation does not provide procurement-grade cryptographic signing and independent verification | evidence-pack libraries/functions/UI | Render real PDF; canonical manifest; SHA-256; asymmetric signature using managed key; verifier and key-rotation procedure | Determinism, tamper, export and independent verification tests | Generate and verify pack outside Quantivis | KMS/signing key, legal approval of evidence format | Not ready |
| Demo shows contradictory expired session and scripted tenant state | High | Root-caused 2026-08-06: `src/pages/Demo.tsx` called `supabase.auth.signOut()` directly instead of `AuthContext`'s `signOut()` wrapper. The global listener in `src/contexts/AuthContext.tsx` (`deliberateSignOutRef`) fires a "Your session ended / Please sign in again" toast for any `SIGNED_OUT` event not explicitly marked deliberate — since Demo.tsx's call bypassed the wrapper, every demo provisioning run raced that toast on top of the Acme Corp scenario card | `src/pages/Demo.tsx`, `src/contexts/AuthContext.tsx` | Demo.tsx now calls `useAuth().signOut()`, which sets `deliberateSignOutRef.current = true` before signing out, suppressing the unexpected-signout toast; removed the now-redundant duplicate `sessionStorage` clears (`signOut()`'s `clearTenantSession()` already does this) | `src/test/demo-session-toast-contradiction.test.ts` (passing); full suite 836/836 passing, build clean | Confirm production `/demo` no longer shows the toast during provisioning | None — this was a pure client-side ordering bug, no infra dependency | Code-complete, deployment unverified |
| Subprocessor page returns empty/loading registry and misleading percentage | High | Database migration/data/function may not be deployed; UI assumes success and policy copy overclaims DPA completion | `Subprocessors.tsx`, registry migration/RPC, DPA/procurement copy | Reliable error/empty states; one verified registry; never infer 100%; reconcile actual vendors | RPC/UI state tests and consistency scan | Compare live resources/contracts to displayed registry | Privacy counsel and vendor DPA evidence | Blocked by external configuration |
| Analytics implementation conflicts with cookie/privacy statements | High | PostHog is present while policy language says no third-party tracking; consent behavior not proven | `src/lib/analytics.ts`, consent UI, Cookies/Privacy/DPA | Default-deny non-essential analytics where required; consent/withdrawal; accurate vendor/purpose/region disclosure | Consent-region, pre-consent network and withdrawal tests | Browser network audit in EU and other configured regions | Privacy counsel approval and PostHog account configuration | Code-complete, deployment unverified |
| Residency/transfer claims conflict | High | Homepage hardcoded "Frankfurt region only" while Security Questionnaire hardcoded "AWS EU-West-1, Ireland" — two different, mutually exclusive AWS regions, neither independently verifiable against the linked Supabase project (`itpwpnwzzitkelffttyx`, not enumerable via available Supabase project access) | `src/pages/Index.tsx`, `src/pages/SecurityQuestionnaire.tsx`, `src/pages/DataResidency.tsx`, new `src/lib/reliability-claims.ts` | Created single source of truth (`CONFIGURED_REGION`, sourced from `VITE_HOSTING_REGION` build env, never hardcoded) reused by all three pages; removed both hardcoded region claims | `src/test/reliability-claims-consistency.test.ts` (5 tests, passing) asserts neither hardcoded literal remains and all three surfaces import the shared module | Confirm the rendered pages show either a consistent `VITE_HOSTING_REGION` value everywhere or the same honest "disclosed under contract" fallback everywhere | None for the code fix; setting `VITE_HOSTING_REGION` to the true verified region is an infra/deploy-config action, not a code change | Code-complete, deployment unverified |
| Backup retention/RPO claims conflict | High | SLA page claimed RPO ≤1h / 30-day retention / continuous backup as unconditional fact; Security Questionnaire claimed RPO 24h best-effort / 7-day retention / daily backup for the same infrastructure — direct numeric contradiction (24x RPO, 4x retention), and neither baseline was independently verified against the linked Supabase project | `src/pages/SLA.tsx`, `src/pages/SecurityQuestionnaire.tsx`, `src/lib/reliability-claims.ts` | Removed unverified baseline numbers from the shared source of truth; production backup frequency, retention, restore-test result, RPO, and RTO now render as not independently verified. SLA recovery objectives remain proposed/contract-dependent only and are not presented as deployed facts | `src/test/reliability-claims-consistency.test.ts` asserts baseline facts are null/unverified and SLA targets are proposed rather than baseline commitments | Confirm rendered SLA and Security Questionnaire pages disclose the unverified recovery posture in production | Independently verifying the actual Supabase backup/PITR schedule and restore-test evidence for the real linked project remains outstanding | Code-complete, deployment unverified |
| Docs overstate production paths and scale | High | `src/data/documentation-sections.ts` asserted "100M+ metric rows across 1,000+ organizations", "sub-500ms"/"within milliseconds" real-time latency, and connector table rows labeled unqualified "Production" — all as already-achieved fact, with no load test or latency telemetry anywhere in the codebase to back them (confirmed against `trust-center.ts`'s own honest Runtime Health section, which reports no live latency/throughput telemetry exists) | `src/data/documentation-sections.ts` (data-architecture, data-ingestion, overview, realtime-streaming sections) | Reworded scale claims as explicit design targets rather than measured results; replaced unverified latency figures with "not independently measured, see /trust"; renamed connector "Production" status to "Implemented" with a defining footnote distinguishing code-wired from load/scale-certified; separately confirmed `src/lib/trust-center.ts`'s Capability Matrix already does this Beta/Live/Not-Deployed labeling correctly for the AG-3/RTS-1 runtime stack (this row of the matrix was stale — that part was already code-complete) | `src/test/documentation-scale-claims.test.ts` (3 tests, passing) locks the softened claims in place | Confirm the rendered /docs page shows the corrected copy in production | None for this fix; a real load/latency benchmark, if ever produced, is a separate future action | Code-complete, deployment unverified |
| Impressum identifiers pending | High | Legal entity identifiers are not available in verified evidence | Impressum/legal pages | Preserve explicit pending state; insert only counsel-verified identifiers | No-placeholder regression test | Counsel verifies live page | Legal registration/VAT evidence | Blocked by external configuration |
| Pricing, annual cost, trial/pilot and unauthenticated plan state conflict | High | Commercial values duplicated across pages, translations and JSON-LD | pricing/compare/register/home/index metadata/i18n/plan UI | Typed commercial source of truth; render all surfaces from it; anonymous users never have a paid plan | Consistency, metadata and auth-state tests | Crawl all public commercial surfaces | Executive approval of final commercial terms | Not ready |
| Protected workflows remain unverified | High evidence gap | Audit had no authenticated tenant; source tests are not deployment proof | application routes, Auth, RLS, functions | Complete role-based E2E matrix and failure states | Cross-browser E2E plus direct API/RLS negatives | Execute with approved multi-role test tenant | Test accounts and environment access | Blocked by external configuration |
| CSP deploy scripts retain `unsafe-eval` | High/Medium security hardening | Cloudflare scripts duplicated a stale CSP instead of consuming the central policy; baseline checker ignored them | Cloudflare scripts, central CSP, verification script | Use one CSP source; ban `unsafe-eval` in every deployable source; phase out `unsafe-inline` with nonces/hashes | Security config and CSP source tests | Verify response header on every major public/auth route | Cloudflare deployment/API token for managed rule | Code-complete, deployment unverified |
| Legal/security claims lack external certification and penetration evidence | High procurement blocker | Code cannot establish legal registration, SOC 2/ISO certification or independent penetration results | marketing, trust/procurement pages | Remove/qualify unsupported claims; evidence registry with expiry/owner | Claim lint and evidence-expiry tests | Inspect signed third-party reports under NDA | Counsel, auditor, penetration-testing vendor | Blocked by external configuration |

## Completion rule

No Critical or High item moves to `Verified in production` without an objective production artifact. No readiness verdict reaches GA-ready while any item above is `Not ready`, `Code-complete, deployment unverified`, or `Blocked by external configuration`.

## Deployment evidence and external blockers

- Main branch commit `b4cd2712f5122c0debbdcf7d4177ea5d15011dbb` had a successful CI run on 2026-08-13: [31704324643](https://github.com/stanleymay20/quantisights-pro-c6abd242/actions/runs/31704324643).
- Supabase deployment remained unverified on the latest Supabase-touching commit; [Deploy Supabase run 31703486994](https://github.com/stanleymay20/quantisights-pro-c6abd242/actions/runs/31703486994) failed and job logs require repository admin rights to inspect.
- On 2026-08-13, `https://www.quantivis.io` returned deployment ID `334c3521-f017-4152-8f25-bd16b87534b2` but the live CSP still contained `unsafe-eval`; therefore the live security-header configuration is not production verified.
- These are release-configuration blockers. They must not be represented as code-complete or silently bypassed with fabricated credentials.
