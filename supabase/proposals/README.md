# Supabase SQL proposals

Files in this directory are security/design artefacts, not deployable database
migrations. They are intentionally excluded from `supabase db push`.

Promote an approved proposal by creating a new migration with `supabase
migration new`, reviewing it against the current schema, and adding live
verification evidence.

